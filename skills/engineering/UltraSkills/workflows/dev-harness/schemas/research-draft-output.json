{
  "title": "DevHarness Research Draft Output",
  "description": "Producer output contract for the DevHarness research draft before critic attack or repeat user approval.",
  "type": "object",
  "required": [
    "outcome",
    "next_step"
  ],
  "properties": {
    "outcome": {
      "enum": [
        "ready_for_attack",
        "ready_for_approval"
      ],
      "description": "Producer must use ready_for_attack for an initial or critic-requested draft and ready_for_approval for a correction requested at the user gate. Missing help is reported through the runner non-blocking stop control channel, never as an output outcome.",
      "x-usage": "Treat this as the authoritative workflow state for this output. Do not infer success from other populated fields when it requires revision."
    },
    "next_step": {
      "enum": [
        "ui_intent_draft",
        "architecture_draft"
      ],
      "description": "Researcher-selected step after human research approval: UI design proposal for material user-facing UI work, otherwise architecture.",
      "x-usage": "Use ui_intent_draft when the research artifact says UI design is needed. Use architecture_draft when it says UI design is not needed. The approval step reads this field directly."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Producer must summarize the research proposal state for human approval without embedding the reasons-canvas-research artifact body.",
      "x-usage": "Use this as the human-facing approval state for the research gate. Show it with the artifact attachment; do not recreate the summary from artifact contents in the orchestrator."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/artifact"
      },
      "description": "Producer must emit artifact metadata for the required immutable reasons-canvas-research artifact when ready for attack or approval.",
      "x-usage": "Emit only the required immutable reasons-canvas-research artifact. Exact artifact field semantics come from the shared baton artifact schema.",
      "minItems": 1,
      "maxItems": 1,
      "contains": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "const": "reasons-canvas-research"
          }
        }
      }
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Producer may emit compact structured results, decisions, or evidence summaries created by this stage.",
      "x-usage": "Use these as supporting evidence or compact summaries. Do not let them override outcome, selected steps or approved contracts."
    }
  },
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MrFlashAccount/Skills/schemas/workflow/dev-harness/research-draft-output",
  "allOf": [
    {
      "if": {
        "properties": {
          "outcome": {
            "enum": [
              "ready_for_attack",
              "ready_for_approval"
            ]
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "required": [
          "summary",
          "artifacts"
        ]
      }
    }
  ]
}
