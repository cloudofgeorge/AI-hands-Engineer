{
  "title": "DevHarness Architecture Draft Output",
  "description": "Producer output contract for the DevHarness architecture draft before critic attack or repeat user approval.",
  "type": "object",
  "required": [
    "outcome"
  ],
  "properties": {
    "outcome": {
      "enum": [
        "ready_for_attack",
        "ready_for_approval"
      ],
      "description": "Producer must use ready_for_attack for an initial or critic-requested draft and ready_for_approval for a correction requested at the user gate. Missing help is reported through the runner non-blocking stop control channel, never as an output outcome.",
      "x-usage": "Treat this as the authoritative workflow state for this output. Do not infer success from other populated fields when it requires revision."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Producer must summarize the architecture proposal state for human approval without embedding the reasons-canvas-architecture artifact body.",
      "x-usage": "Use this as the human-facing approval state for the architecture gate. Show it with the artifact attachment; do not recreate the summary from artifact contents in the orchestrator."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/artifact"
      },
      "description": "Producer must emit artifact metadata for the required immutable reasons-canvas-architecture artifact when ready for attack or approval.",
      "x-usage": "Emit only artifact metadata here; the full architecture contract body lives in the referenced artifact file.",
      "minItems": 1,
      "maxItems": 1,
      "contains": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "const": "reasons-canvas-architecture"
          }
        }
      }
    }
  },
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MrFlashAccount/Skills/schemas/workflow/dev-harness/architecture-draft-output",
  "allOf": [
    {
      "if": {
        "properties": {
          "outcome": {
            "enum": [
              "ready_for_attack",
              "ready_for_approval"
            ]
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "required": [
          "summary",
          "artifacts"
        ]
      }
    }
  ]
}
